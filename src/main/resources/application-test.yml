server:
  port: 8081

spring:
  datasource:
    url: jdbc:h2:mem:eventhubtestdb
    driver-class-name: org.h2.Driver
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false

logging:
  level:
    root: DEBUG